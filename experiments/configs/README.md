# Experiment configurations

Per-phase experiment matrices for the study. One subdirectory per phase
(`phase1` … `phase5`, `h6`); each holds the configuration files that define the
runs for that phase. Files here are data (YAML/JSON), not code.
