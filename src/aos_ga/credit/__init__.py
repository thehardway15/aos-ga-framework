"""Credit-assignment schemes.

Maps the outcome of an operator application to a non-negative reward consumed by
the AOS strategies: instantaneous reward, rewarded-fitness-improvement and
rank-based variants. Operates on the unified quality function ``g(x)`` so the
reward sign is consistent across problem classes.
"""
