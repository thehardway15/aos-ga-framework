"""Generic genetic operators acting on the core representations.

Reusable crossover and mutation operators for permutation, binary and
real-valued genomes, together with the operator-pool registry from which the
AOS engine draws. The operators are problem-agnostic: they transform a
representation and know nothing about a specific test problem.
"""
